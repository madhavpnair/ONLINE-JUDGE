{
  "name": "oj",
  "version": "1.0.0",
  "description": "An online platform to host CP contests",
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "nodemon server/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "nodemon": "^3.0.0"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "react-router-dom": "^6.30.1"
  }
}
